function func() {}

function sum(sum1, sum2) {
	return sum1 + sum2;
}

console.log(func());

const obj = {
	// 메서드
	prop: function () {},
};

// 생성자 함수
function MyObj() {}

// 화살표 함수(ES2015+, ES6)
const arrowFunc = () => {};

sum(1, 2);
