/**
 * 삼항 연산자
 *  ㄴ 3개의 피연산자, 조건 연산자
 */
const val = '조건' ? '참일때' : '거짓일때';

let var2;

if ('조건') {
	var2 = '참일때';
} else {
	var2 = '거짓일때';
}

const age = 20;
const isAdult = age >= 20 ? '성인' : '미성년자';

/*
if (isAdult) {
  for {

  }

  while () {

  }

  switch () {
    case value:

      break;

    default:
      break;
  }

  if () {

  }
}
*/
